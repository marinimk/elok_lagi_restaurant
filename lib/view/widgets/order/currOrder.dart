import 'package:flutter/material.dart';

class CurrOrder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Marini buat order'),
      ),
      body: Container(),
    );
  }
}