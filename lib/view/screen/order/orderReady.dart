import 'package:flutter/material.dart';

class OrderReady extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Your order is ready baby')),
        body: Center(child: Container(child: Text(':\'D'))));
  }
}
